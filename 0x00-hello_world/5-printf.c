#include <stdio.h>
/*
 * Writing 
 * a printf function obtained from the stdio library
 * and returns the value 0
 * when successful
 */
int main(void)
{
     printf("with proper grammar, but the outcome is a piece of art,");
     return (0);
}
